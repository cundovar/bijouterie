{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_index') }}">retour</a>

    <a href="{{ path('app_admin_categorie_edit', {'id': categorie.id}) }}">editer</a>

    {{ include('admin_categorie/_delete_form.html.twig') }}
{% endblock %}
